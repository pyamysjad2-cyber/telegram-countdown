name: Daily Telegram Bot

on:
  schedule:
    - cron: '30 20 * * *'
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: uses:actions/checkout@v5

      - name: Setup Python
        uses: uses:actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install Telegram library
        run: pip install python-telegram-bot

      - name: Run bot
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: python bot.py
